sudo apt install flex,bison,clang

which flex 
#available on machine ot not

apt show flex 
#shows the version available for downlaod


flex ssc.l
clang++ lex.yy.c
./a.out input.ssc 

bison -d ssc.y



git init
git config
git status
git add
git commit
git branch #use -d for deletion of it as well -r for remote
git checkout
git log
gut merge branch_name
git reset HEAD~
git branch --unset-upstream